1. when too much text is entered in calculator textarea then it shows scroll bar , so remove it and if user then enter something to apne aap wha pe focus ho like entering digits dikhe na ki old digits jo ki scrolling me upr ja chuki h (bascially auto scroll)
2. lets say we desgin whole app and we hve not add functionality to some things , so on clicking them we can return  a toaster with msg, featue coming soon




    // Make window draggable and bring it to front when dragging starts
        useGSAP(() => {
            const el = ref.current;

            // Draggable.create() returns an array of draggable instances.
            // Since we are creating only one draggable element, we extract the first instance.
            const [instance] = Draggable.create(el, { onPress: () => dispatch(focusWindow({ windowKey })) });

            // Cleanup draggable instance when component unmounts
            // This removes event listeners and prevents memory leaks
            return () => instance.kill();
        }, [])




        